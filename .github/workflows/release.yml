name: Build & Release

# Chạy khi push một tag phiên bản (vd: v1.0.2). Commit thường KHÔNG kích hoạt.
on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write        # cần để tạo Release + upload file

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pyinstaller

      - name: Verify release version
        shell: pwsh
        run: |
          $version = (Select-String -Path gui.py -Pattern '^APP_VERSION = "([0-9]+\.[0-9]+\.[0-9]+)"$').Matches.Groups[1].Value
          if ("v$version" -ne "${{ github.ref_name }}") { throw "Tag and APP_VERSION differ" }

      - name: Build executable
        run: pyinstaller AutoVisionClicker.spec --noconfirm --clean

      - name: Rename with version
        run: Copy-Item dist/AutoCatchPokemonPGSharp.exe "dist/AutoCatchPokemonPGSharp-${{ github.ref_name }}.exe"

      - name: Write update checksum
        shell: pwsh
        run: |
          $hash = (Get-FileHash -LiteralPath dist/AutoCatchPokemonPGSharp.exe -Algorithm SHA256).Hash.ToLowerInvariant()
          Set-Content -LiteralPath dist/AutoCatchPokemonPGSharp.exe.sha256 -Value "$hash  AutoCatchPokemonPGSharp.exe" -Encoding ascii

      - name: Prepare notes for this version
        shell: pwsh
        run: |
          $notes = Get-Content RELEASE_NOTES.md -Raw -Encoding UTF8
          $match = [regex]::Match($notes, '(?s)\A(.*?)(?=\r?\n# v\d+\.\d+\.\d+\s*\r?\n|\z)')
          if (-not $match.Success -or -not $match.Groups[1].Value.StartsWith("# ${{ github.ref_name }}")) {
            throw "Release notes do not begin with this version"
          }
          Set-Content -LiteralPath release-current.md -Value $match.Groups[1].Value.Trim() -Encoding UTF8

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: >
          gh release create ${{ github.ref_name }}
          "dist/AutoCatchPokemonPGSharp.exe"
          "dist/AutoCatchPokemonPGSharp-${{ github.ref_name }}.exe"
          "dist/AutoCatchPokemonPGSharp.exe.sha256"
          "downloads/discord-coord-collector-v0.3.3.zip"
          --title ${{ github.ref_name }}
          --notes-file release-current.md

      - name: Notify Discord about the new release
        continue-on-error: true
        shell: pwsh
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          RELEASE_VERSION: ${{ github.ref_name }}
          RELEASE_URL: https://github.com/${{ github.repository }}/releases/tag/${{ github.ref_name }}
          DOWNLOAD_URL: https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/AutoCatchPokemonPGSharp-${{ github.ref_name }}.exe
        run: |
          if ([string]::IsNullOrWhiteSpace($env:DISCORD_WEBHOOK_URL)) {
            Write-Warning "DISCORD_WEBHOOK_URL is not configured; skipping Discord notification."
            exit 0
          }

          # Show the Vietnamese notes for only the newest version directly in Discord.
          # A field is limited to 1,024 characters, so leave room for the full-notes link.
          $releaseNotes = Get-Content RELEASE_NOTES.md -Raw -Encoding UTF8
          $match = [regex]::Match(
            $releaseNotes,
            '(?s)\A#\s+[^\r\n]+\s+##\s+Tiếng Việt\s+(.*?)(?:\r?\n---)'
          )
          if ($match.Success) {
            $changeDetails = $match.Groups[1].Value.Trim()
            $changeDetails = [regex]::Replace(
              $changeDetails,
              '(?m)^###\s+(.+)$',
              '**$1**'
            )
          } else {
            $changeDetails = "Xem danh sách thay đổi đầy đủ trên GitHub."
          }
          if ($changeDetails.Length -gt 850) {
            $changeDetails = $changeDetails.Substring(0, 847).TrimEnd() + "..."
          }
          $changeDetails += "`n`n[Xem đầy đủ trên GitHub]($($env:RELEASE_URL))"

          $embed = @{
            title       = "Auto Catch Pokemon PGSharp $($env:RELEASE_VERSION)"
            description = "Bản cập nhật mới đã sẵn sàng. Tải file EXE mới và thay thế bản cũ; không cần cài đặt lại."
            url         = $env:RELEASE_URL
            color       = 5763719
            fields      = @(
              @{
                name   = "Tải bản cập nhật"
                value  = "[AutoCatchPokemonPGSharp-$($env:RELEASE_VERSION).exe]($($env:DOWNLOAD_URL))"
                inline = $false
              },
              @{
                name   = "Chi tiết thay đổi"
                value  = $changeDetails
                inline = $false
              }
            )
            footer      = @{ text = "Auto Catch Pokemon PGSharp" }
            timestamp   = (Get-Date).ToUniversalTime().ToString("o")
          }
          $payload = @{
            username = "Auto Catch Update"
            embeds   = @($embed)
          } | ConvertTo-Json -Depth 6

          Invoke-RestMethod `
            -Uri $env:DISCORD_WEBHOOK_URL `
            -Method Post `
            -ContentType "application/json; charset=utf-8" `
            -Body ([Text.Encoding]::UTF8.GetBytes($payload))
